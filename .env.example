# Copy to .env and fill in values before running with Docker

# PostgreSQL password for the database container
POSTGRES_PASSWORD=change_me_in_production

# Secret used for session signing — use a long random string
SESSION_SECRET=change_me_in_production

# Port to expose the app on (default: 5000)
APP_PORT=5000
